{
  "name": "smartgarden-optimism-gauges",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ smartgarden-optimism-gauges",
    "lfg": "graph codegen && graph build && graph deploy --studio smartgarden-optimism-gauges",
    "create-local": "graph create --node http://localhost:8020/ smartgarden-optimism-gauges",
    "remove-local": "graph remove --node http://localhost:8020/ smartgarden-optimism-gauges",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 smartgarden-optimism-gauges",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "*",
    "@graphprotocol/graph-ts": "*"
  }
}
