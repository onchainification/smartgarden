{
  "name": "smartgarden",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ smartgarden",
    "lfg": "graph codegen && graph build && graph deploy --studio smartgarden",
    "create-local": "graph create --node http://localhost:8020/ smartgarden",
    "remove-local": "graph remove --node http://localhost:8020/ smartgarden",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 smartgarden",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.54.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}
